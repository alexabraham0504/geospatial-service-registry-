
services:
  # ── Node.js Backend API ──────────────────────────────────────────────────
  app:
    build: .
    container_name: geospatial-api
    ports:
      - "5000:5000"
    # Load the MongoDB Atlas connection string from your local .env file
    env_file:
      - .env
    restart: unless-stopped
